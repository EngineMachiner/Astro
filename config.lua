---@class AstroConfig
---@field Concat { showID:boolean, showIndex:boolean, wideMode:boolean, indentation:string, keyQuotes:boolean }
return {
    Concat = {
        showID = false,
        showIndex = true,
        wideMode = true,
        indentation = '	',
        keyQuotes = true,
    }
}
